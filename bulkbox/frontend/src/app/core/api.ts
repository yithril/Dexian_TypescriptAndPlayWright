// Base path for the json-server mock API. The Angular dev server proxies
// "/api" to the json-server instance (see proxy.conf.json), so the browser
// never has to deal with a cross-origin port.
export const API_BASE = '/api';
