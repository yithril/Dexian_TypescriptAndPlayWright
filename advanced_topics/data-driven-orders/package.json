{
  "name": "data-driven-orders",
  "version": "1.0.0",
  "description": "Playwright example: data-driven tests from CSV and Excel files.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "generate:xlsx": "node scripts/generate-xlsx.mjs"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.0.0"
  }
}
