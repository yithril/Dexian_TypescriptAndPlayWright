{
  "name": "messy-locators",
  "version": "1.0.0",
  "description": "Playwright example: locating elements on messy legacy markup without data-testids.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.0.0"
  }
}
