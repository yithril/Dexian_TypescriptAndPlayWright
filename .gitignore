# Dependencies
node_modules/

# Build output
dist/
out-tsc/
.angular/
*.tsbuildinfo

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/
blob-report/

# Test / coverage
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp files
tmp/
*.tmp

# Editor / IDE
.idea/
*.swp

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Local secrets - never commit
*.key
.env
.env.local
.env.*.local

# Office temp lock files
~$*

# Instructor-only materials (NOT for students)
TEACHING_PLAN.md
PAGETURNER_CODEALONG.md
INSTRUCTOR_NOTES.md

# Whole instructor-notes folder is private (answer keys, run-of-show, etc.)
teachers_notes/

# Lab answer key - kept OUT of git for now. To publish it later, delete this
# line, then `git add lab2-solution` and commit (its history starts only then).
/lab2-solution/

# One-off tooling (remove after the images are fetched)
bulkbox/scripts/
